# Base runtime dependencies — fast to install, sufficient for the app to boot
# and serve all routes (grading falls back to a deterministic stub if
# requirements-ml.txt isn't installed; see grading/pipeline.py).
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0
psycopg[binary]>=3.2
APScheduler>=3.11
python-multipart>=0.0.20
httpx>=0.28
pydantic>=2.10
pydantic-settings>=2.7
